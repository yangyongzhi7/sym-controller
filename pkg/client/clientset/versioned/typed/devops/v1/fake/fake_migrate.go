/*
Copyright The Symphony Authors.

*/

// Code generated by client-gen. DO NOT EDIT.

package fake

import (
	devopsv1 "github.com/yangyongzhi/sym-operator/pkg/apis/devops/v1"
	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	labels "k8s.io/apimachinery/pkg/labels"
	schema "k8s.io/apimachinery/pkg/runtime/schema"
	types "k8s.io/apimachinery/pkg/types"
	watch "k8s.io/apimachinery/pkg/watch"
	testing "k8s.io/client-go/testing"
)

// FakeMigrates implements MigrateInterface
type FakeMigrates struct {
	Fake *FakeDevopsV1
	ns   string
}

var migratesResource = schema.GroupVersionResource{Group: "devops.dmall.com", Version: "v1", Resource: "migrates"}

var migratesKind = schema.GroupVersionKind{Group: "devops.dmall.com", Version: "v1", Kind: "Migrate"}

// Get takes name of the migrate, and returns the corresponding migrate object, and an error if there is any.
func (c *FakeMigrates) Get(name string, options v1.GetOptions) (result *devopsv1.Migrate, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewGetAction(migratesResource, c.ns, name), &devopsv1.Migrate{})

	if obj == nil {
		return nil, err
	}
	return obj.(*devopsv1.Migrate), err
}

// List takes label and field selectors, and returns the list of Migrates that match those selectors.
func (c *FakeMigrates) List(opts v1.ListOptions) (result *devopsv1.MigrateList, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewListAction(migratesResource, migratesKind, c.ns, opts), &devopsv1.MigrateList{})

	if obj == nil {
		return nil, err
	}

	label, _, _ := testing.ExtractFromListOptions(opts)
	if label == nil {
		label = labels.Everything()
	}
	list := &devopsv1.MigrateList{ListMeta: obj.(*devopsv1.MigrateList).ListMeta}
	for _, item := range obj.(*devopsv1.MigrateList).Items {
		if label.Matches(labels.Set(item.Labels)) {
			list.Items = append(list.Items, item)
		}
	}
	return list, err
}

// Watch returns a watch.Interface that watches the requested migrates.
func (c *FakeMigrates) Watch(opts v1.ListOptions) (watch.Interface, error) {
	return c.Fake.
		InvokesWatch(testing.NewWatchAction(migratesResource, c.ns, opts))

}

// Create takes the representation of a migrate and creates it.  Returns the server's representation of the migrate, and an error, if there is any.
func (c *FakeMigrates) Create(migrate *devopsv1.Migrate) (result *devopsv1.Migrate, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewCreateAction(migratesResource, c.ns, migrate), &devopsv1.Migrate{})

	if obj == nil {
		return nil, err
	}
	return obj.(*devopsv1.Migrate), err
}

// Update takes the representation of a migrate and updates it. Returns the server's representation of the migrate, and an error, if there is any.
func (c *FakeMigrates) Update(migrate *devopsv1.Migrate) (result *devopsv1.Migrate, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewUpdateAction(migratesResource, c.ns, migrate), &devopsv1.Migrate{})

	if obj == nil {
		return nil, err
	}
	return obj.(*devopsv1.Migrate), err
}

// UpdateStatus was generated because the type contains a Status member.
// Add a +genclient:noStatus comment above the type to avoid generating UpdateStatus().
func (c *FakeMigrates) UpdateStatus(migrate *devopsv1.Migrate) (*devopsv1.Migrate, error) {
	obj, err := c.Fake.
		Invokes(testing.NewUpdateSubresourceAction(migratesResource, "status", c.ns, migrate), &devopsv1.Migrate{})

	if obj == nil {
		return nil, err
	}
	return obj.(*devopsv1.Migrate), err
}

// Delete takes name of the migrate and deletes it. Returns an error if one occurs.
func (c *FakeMigrates) Delete(name string, options *v1.DeleteOptions) error {
	_, err := c.Fake.
		Invokes(testing.NewDeleteAction(migratesResource, c.ns, name), &devopsv1.Migrate{})

	return err
}

// DeleteCollection deletes a collection of objects.
func (c *FakeMigrates) DeleteCollection(options *v1.DeleteOptions, listOptions v1.ListOptions) error {
	action := testing.NewDeleteCollectionAction(migratesResource, c.ns, listOptions)

	_, err := c.Fake.Invokes(action, &devopsv1.MigrateList{})
	return err
}

// Patch applies the patch and returns the patched migrate.
func (c *FakeMigrates) Patch(name string, pt types.PatchType, data []byte, subresources ...string) (result *devopsv1.Migrate, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewPatchSubresourceAction(migratesResource, c.ns, name, data, subresources...), &devopsv1.Migrate{})

	if obj == nil {
		return nil, err
	}
	return obj.(*devopsv1.Migrate), err
}
